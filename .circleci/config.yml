version: 2.1

orbs:
  python: circleci/python@0.2.1

jobs:
  build:
    docker:
      - image: google/cloud-sdk
      - image: docker:18.09-git
    steps:
      - checkout
      - setup_remote_docker
      - run:
         echo ${GOOGLE_AUTH} > ${HOME}/gcp-key.json
      - run:
          name: Build image
          command: docker build -t us.gcr.io/dsp-appsec-dev/example-app-test:${CIRCLE_SHA1} .
      - run:
          name: Install trivy
          command: |
            apk add --update-cache --upgrade --update curl
            curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/master/contrib/install.sh | sh -s -- -b /usr/local/bin
      - run:
          name: Scan the local image with trivy (light)
          command: trivy --exit-code 0 --no-progress example-app:${CIRCLE_SHA1}
      - run: 
          gcloud docker -- push us.gcr.io/dsp-appsec-dev/example-app-test:$CIRCLE_SHA1
workflows:
  version: 2
  release:
    jobs:
      - build
